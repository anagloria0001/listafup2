#include <stdlib.h>
#include <stdio.h>

#define N 10

int L[N] = {1,2,1,3,1,4,1,5,1,6};

int resposta;

// -- escreva seu código abaixo dessa linha --- (e não altere esta linha)


int main() {
    int i;
    for(i=0;i<N;i++){
        if(L[i]==L[i+1]){
        resposta=1;
        break;}
        else{
            resposta=0;
        }
    }
    printf("%d", resposta);   


}
